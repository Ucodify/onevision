import React from "react";
import Home from "../Home";
import { Box } from "@mui/material";

const Location = () => {
  
  return (
    <Box className='bg' sx={{ display: { xs: "block", md: "flex" } }}>
      <Home />
    </Box>
  );
};

export default Location;
